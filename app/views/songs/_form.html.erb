<%= form_with model: song, local: true do |form| %>
  <%= render 'shared/errors', errors: song.errors %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control', required: true %>
  </div>

  <div class="form-group">
    <%= form.label :artist %>
    <%= form.text_field :artist, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :url %>
    <%= form.url_field :url, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :notes %>
    <%= form.text_area :notes, class: 'form-control' %>
    <small class="form-text text-muted">
      想定している楽器編成やアレンジ、Key などを入力してください。
    </small>
  </div>

  <div class="form-check mb-3">
    <%= form.check_box :closed, class: 'form-check-input' %>
    <%= form.label :closed, class: 'form-check-label' %>
  </div>

  <%= form.submit class: 'btn btn-primary' %>
<% end %>
